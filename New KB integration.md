
Make two tabs:

- Direct Data Sources
- Milvus wala 

Make a separate page for data source connectors from where you can configure Direct Data Sources and Milvus ones as well.

Store requirement metadata on App database to know which Trino catalog is connected to which database.

payload ma `query_metadata` bhanne rakhne.